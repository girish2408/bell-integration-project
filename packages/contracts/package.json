{
  "name": "@pay/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Zod schemas and inferred types. The single source of truth for the API contract.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
